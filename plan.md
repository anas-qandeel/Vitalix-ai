# توضيح: لا توجد رسالة واتساب تستخدم days_left بصيغة خاطئة

## السطر ~435 ليس رسالة واتساب

النص الكامل والسياق:
```tsx
{isExpiringSoon && (
  <div className="mt-5 bg-amber-50 border border-amber-200 rounded-xl px-4 py-3 flex items-center gap-3 w-fit shadow-sm">
    ...
    <div>
      <p className="text-amber-900 text-xs font-bold">تنبيه الاشتراك</p>
      <p className="text-amber-700 text-[10px] mt-0.5 font-medium">ينتهي خلال {pluralizeDays(daysLeft)}</p>
    </div>
  </div>
)}
```

هذا **بادج "تنبيه الاشتراك"** — يظهر على الشاشة فقط للصيدلاني نفسه، يُخبره بعدد الأيام المتبقية لانتهاء **اشتراك الصيدلية** (`pharmacy.expiry_date`)، وليس له أي علاقة بمواعيد أدوية المرضى أو برسائل واتساب تُرسَل لهم. `daysLeft` هنا متغيّر مختلف تماماً (سطر 308):
```ts
const daysLeft = !pharmacy?.expiry_date ? 0 : Math.max(0, Math.ceil((new Date(pharmacy.expiry_date).getTime() - Date.now()) / 86400000));
```
لا يزال فيه `Math.max(0,...)` خاص به (لم يُلمس، وليس ضمن أي اتفاق سابق)، ومحاط بشرط `isExpiringSoon` (`daysLeft <= 14`) — أي لا يظهر إطلاقاً إلا حين الاشتراك قريب الانتهاء، ولا يمكن أن يصل لقيمة سالبة عملياً بسبب `Math.max(0,...)` الخاص به.

## رسالة الواتساب الفعلية المرسلة للمريض

بحث مجدد عن أي `msg`/نص يُبنى من `days_left` أو `pluralizeDays` لهذا الغرض في `dashboard/page.tsx`:
```
147, 525: رسائل أعياد الميلاد فقط (لا علاقة بالأدوية)
300-304: handleWaConfirm — يستقبل customMsg جاهزاً من WaMsgModal ويفتح wa.me به مباشرة، لا يبني نصاً بنفسه
```

**لا يوجد أي كود في `dashboard/page.tsx` يبني نص تذكير من `days_left`.** الرسالة الفعلية المرسلة تُبنى بالكامل داخل `src/components/WaMsgModal.tsx` (المكوّن المشترك مع chronic)، وهي نص عام لا يذكر رقم الأيام إطلاقاً ("...سيكفيكم لأيام قليلة...")، وقائمة الأدوية المعروضة **داخل** تلك النافذة تتعامل مع التأخر بشكل صحيح مسبقاً (`d <= 0 ? 'نفد!' : ...`) — هذا ما وُثِّق سابقاً في `plan.md` وحصل على موافقة صريحة.

## الخلاصة

لا "msg" بصيغة "ينفد خلال متأخر 3 أيام" موجود في أي مكان بالكود حالياً:
- لا في السطر 435 (بادج اشتراك الصيدلية، غير متعلق بالمرضى إطلاقاً).
- لا في مسار رسالة المريض (نص عام في `WaMsgModal` لا يذكر الأيام، والقائمة الداخلية للأدوية تتعامل مع التأخر بشكل صحيح مسبقاً).

لم يُطبَّق أي تعديل. بانتظار توضيح إن كان المقصود شيئاً آخر ظهر فعلياً في الواجهة.
