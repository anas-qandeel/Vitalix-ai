# استبدال النص العامي بنص فصيح — حالة "اتصل اليوم" الفارغة

## 1. بحث عن الجملة العامية "ما في حدا بحاجة تذكير اليوم"

بحثت في كل الملفات تحت `src/` (dashboard, chronic, admin, المكوّنات المشتركة، صفحات المريض العامة). **موضع واحد فقط**:

| الملف | السطر | النص الحالي |
|---|---|---|
| `src/app/dashboard/page.tsx` | 409 | `<h3 className="text-base font-bold text-slate-900">ما في حدا بحاجة تذكير اليوم</h3>` |

هذا داخل حالة العرض الفارغة (empty state) لبطاقة "اتصل اليوم" في الشاشة الرئيسية، عندما تكون `todayAlerts.length === 0`.

لا يوجد نفس النص أو ما يشبهه في `chronic/page.tsx` أو أي مكوّن مشترك (`WaMsgModal.tsx`, `DashboardHeader.tsx`, `AppFooter.tsx`) أو صفحات الأدمن.

## 2. بحث عن نصوص عامية أخرى ظاهرة للمستخدم

بحثت عن الكلمات الدارجة الشائعة التالية عبر كل `src/`: `ما في`، `شو`، `هلق`، `بدك`، `كتير`، `مافي`، `إشي`، `هاد`/`هاي`، `منيح`، `لسا`، `حدا`، `احنا`، `منشان`/`عشان`.

**لا نتائج فعلية أخرى.** كل التطابقات الأخرى التي ظهرت في البحث كانت تشابهاً جزئياً في كلمات فصيحة (مثال: "عشوائي" يحتوي "شو"، "فيها"/"فيها البيانات" يحتوي "ما في" كسلسلة فرعية، "السابقة" تحتوي "لسا" كسلسلة فرعية) — وليست كلمات عامية فعلية. لا نصوص عامية أخرى ظاهرة للمستخدم في الكود.

## الإجراء

استبدال نص واحد فقط، بلا أي تغيير في المنطق أو التصميم:

```diff
- <h3 className="text-base font-bold text-slate-900">ما في حدا بحاجة تذكير اليوم</h3>
+ <h3 className="text-base font-bold text-slate-900">لا يوجد مرضى بحاجة للتذكير اليوم</h3>
```

الملف: `src/app/dashboard/page.tsx`، السطر 409.
